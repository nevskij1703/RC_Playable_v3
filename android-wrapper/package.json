{
  "name": "rc-playable-android",
  "version": "1.0.0",
  "description": "Android wrapper for RC Playable v3",
  "private": true,
  "scripts": {
    "sync": "cap sync android",
    "build:debug": "cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  }
}
